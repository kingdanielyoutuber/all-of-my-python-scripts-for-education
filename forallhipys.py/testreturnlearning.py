def add_numbers(a, b):
    return a + b #This function takes two parameters (a and b), adds them together, and returns the result. When calling add_numbers(5, 3), it returns 8, which is then printed.

# Example usage
result = add_numbers(5, 3)
print("The sum is:", result)